public class PlayerGravPlanetJumpState : PlayerAbilityState
{
    public PlayerGravPlanetJumpState(Player player, PlayerStateMachine stateMachine, PlayerData playerData, string animBoolName) : base(player, stateMachine, playerData, animBoolName)
    {
    }
    
    //TODO - this
}